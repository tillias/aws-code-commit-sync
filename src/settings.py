import os

HASH_EXTENSION = '.hash'
PACKAGE_EXTENSION = '.zip'

# Where packages will be downloaded
DOWNLOADS = 'c:/tmp'

# Where packages will be extracted before pushing into AWS CodeCommit
EXTRACT_PATH = 'c:/tmp'
